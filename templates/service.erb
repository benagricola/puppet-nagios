
define service {
    host_name           <%= scope.lookupvar('host_name') %>
    service_description <%= scope.lookupvar('real_service_description') %>
    check_command       <%= scope.lookupvar('real_check_command') %>
    use                 <%= scope.lookupvar('use') %>
}
