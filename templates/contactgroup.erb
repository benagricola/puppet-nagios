# File Managed by Puppet
define contactgroup{
    contactgroup_name               <%= scope.lookupvar('name') %>
    alias                           <%= scope.lookupvar('alias') %>
    members                         <%= scope.lookupvar('members') %>
}
